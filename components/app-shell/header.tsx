"use client";
/* eslint-disable react-hooks/set-state-in-effect */
import { Bell, ChevronDown, LogOut, Menu, Settings, ShieldCheck, UserRound } from "lucide-react";
import { useEffect, useRef, useState } from "react";
import { usePathname, useRouter } from "next/navigation";
import { endSession, getSession, initials, canAccessAdmin, setDemoWorkspace, type SessionUser } from "@/lib/auth";
import { ThemeToggle } from "./theme-toggle";
export function Header({title,onOpenNavigation}:{title:string;onOpenNavigation:()=>void}) { const [notice,setNotice]=useState(false), [open,setOpen]=useState(false), [user,setUser]=useState<SessionUser|null>(null); const ref=useRef<HTMLDivElement>(null); const router=useRouter(); const admin=usePathname().startsWith("/admin"); useEffect(()=>{setUser(getSession()); const close=(e:MouseEvent)=>{if(ref.current&&!ref.current.contains(e.target as Node)) setOpen(false)}; document.addEventListener("mousedown",close); return()=>document.removeEventListener("mousedown",close)},[]); const switchTo=(role:"student"|"placement_officer")=>{if(setDemoWorkspace(role)) router.push(role==="student"?"/dashboard":"/admin"); setOpen(false)}; return <header className="relative flex h-15 shrink-0 items-center justify-between border-b border-border bg-background/95 px-4 backdrop-blur sm:px-6"><div className="flex min-w-0 items-center gap-3"><button onClick={onOpenNavigation} className="grid size-9 place-items-center rounded-lg text-muted-foreground hover:bg-muted lg:hidden" aria-label="Open navigation"><Menu className="size-5"/></button><div><div className="flex items-center gap-2"><h1 className="truncate text-base font-semibold tracking-tight sm:text-lg">{title}</h1>{admin&&<span className="hidden items-center gap-1 rounded-full bg-primary/10 px-2 py-0.5 text-[10px] font-bold text-primary sm:inline-flex"><ShieldCheck className="size-3"/>PLACEMENT OFFICER</span>}</div><p className="hidden text-[11px] text-muted-foreground md:block">{admin?"Institutional career intelligence":"Your career intelligence workspace"}</p></div></div><div className="flex items-center gap-1.5 sm:gap-2"><ThemeToggle/><button onClick={()=>setNotice(!notice)} className="relative grid size-9 place-items-center rounded-lg text-muted-foreground hover:bg-muted" aria-label="View notifications"><Bell className="size-4"/><span className="absolute right-2 top-2 size-1.5 rounded-full bg-primary ring-2 ring-background"/></button><div ref={ref}><button onClick={()=>setOpen(!open)} className="hidden items-center gap-2 rounded-lg px-2 py-1.5 hover:bg-muted sm:flex" aria-expanded={open}><span className="flex size-7 items-center justify-center rounded-full bg-primary/10 text-[10px] font-semibold text-primary">{initials(user)}</span><ChevronDown className="size-3.5 text-muted-foreground"/></button>{open&&<div className="absolute right-4 top-13 z-40 w-64 rounded-xl border border-border bg-popover p-2 shadow-xl"><div className="border-b border-border px-2 pb-3"><p className="truncate text-sm font-semibold">{user?.fullName||user?.name||"Student"}</p><p className="truncate text-xs text-muted-foreground">{user?.email}</p><p className="mt-1 text-[11px] font-medium text-primary">{user?.role==="placement_officer"?"Placement Officer":"Student"}</p></div><button onClick={()=>router.push("/settings")} className="mt-2 flex w-full items-center gap-2 rounded-lg px-2 py-2 text-sm hover:bg-muted"><UserRound className="size-4"/>Profile</button>{canAccessAdmin(user)&&<><button onClick={()=>switchTo("student")} className="flex w-full items-center gap-2 rounded-lg px-2 py-2 text-sm hover:bg-muted">Student Workspace</button><button onClick={()=>switchTo("placement_officer")} className="flex w-full items-center gap-2 rounded-lg px-2 py-2 text-sm hover:bg-muted">Placement Command Center</button></>}<button onClick={()=>router.push("/settings")} className="flex w-full items-center gap-2 rounded-lg px-2 py-2 text-sm hover:bg-muted"><Settings className="size-4"/>Settings</button><button onClick={()=>{endSession();router.replace("/login")}} className="flex w-full items-center gap-2 rounded-lg px-2 py-2 text-sm hover:bg-muted"><LogOut className="size-4"/>Log out</button></div>}</div></div>{notice&&<div className="absolute right-4 top-13 z-30 w-72 rounded-xl border border-border bg-card p-3 text-sm shadow-lg"><p className="font-semibold">Your next focus is ready</p><p className="mt-1 text-xs leading-5 text-muted-foreground">Continue System Design Fundamentals to keep your readiness momentum.</p></div>}</header> }
