"use client";
import { Monitor, Moon, Sun } from "lucide-react";
import { useEffect, useState } from "react";
import { cn } from "@/lib/utils";
type Theme = "light" | "dark" | "system";
export function ThemeToggle() { const [theme, setTheme] = useState<Theme>("system"); useEffect(() => { const timer = window.setTimeout(() => setTheme((localStorage.getItem("skillora-theme") as Theme) || "system"), 0); return () => window.clearTimeout(timer); }, []); function choose(value: Theme) { setTheme(value); localStorage.setItem("skillora-theme", value); document.documentElement.classList.toggle("dark", value === "dark" || (value === "system" && window.matchMedia("(prefers-color-scheme: dark)").matches)); } const Icon = theme === "dark" ? Moon : theme === "light" ? Sun : Monitor; return <div className="relative group"><button type="button" className="inline-flex size-9 items-center justify-center rounded-lg text-muted-foreground transition-colors hover:bg-muted hover:text-foreground focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50" aria-label="Change color theme"><Icon className="size-4" /></button><div className="invisible absolute right-0 top-10 z-40 flex w-31 flex-col gap-1 rounded-xl border border-border bg-popover p-1.5 opacity-0 shadow-lg transition group-focus-within:visible group-focus-within:opacity-100 group-hover:visible group-hover:opacity-100">{(["light", "dark", "system"] as Theme[]).map(value => <button key={value} onClick={() => choose(value)} className={cn("rounded-lg px-2 py-1.5 text-left text-xs capitalize", theme === value ? "bg-primary/10 font-semibold text-primary" : "text-muted-foreground hover:bg-muted")}>{value}</button>)}</div></div>; }
