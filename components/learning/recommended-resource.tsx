"use client";

import { Check, ChevronLeft, ChevronRight, ExternalLink, Play, Sparkles } from "lucide-react";
import { useEffect, useState } from "react";
import { Button } from "@/components/ui/button";

type Recommendation = { title: string; meta: string; duration: string; gap: string; description: string; rationale: string; href: string };
const recommendations: Recommendation[] = [
  { title: "System Design Fundamentals", meta: "Curated learning resource · Beginner", duration: "18 min", gap: "32-point gap", description: "Start with the core concepts behind reliable, scalable systems before your next guided practice session.", rationale: "Your System Design confidence is 38% against a 70% role benchmark. This resource focuses on components, scale, and trade-offs.", href: "https://www.youtube.com/results?search_query=system+design+fundamentals" },
  { title: "Data Structures: Trees & Graphs", meta: "Interview practice resource · Intermediate", duration: "24 min", gap: "26-point gap", description: "Strengthen the traversal and search patterns that come up in technical interviews.", rationale: "Data Structures is one of your largest gaps. Build recognition for common tree and graph patterns before timed practice.", href: "https://www.youtube.com/results?search_query=data+structures+trees+graphs+interview" },
  { title: "Clear Technical Interview Answers", meta: "Communication resource · Beginner", duration: "14 min", gap: "26-point gap", description: "Practice explaining your reasoning with a concise, interviewer-friendly structure.", rationale: "Interview communication improves fastest when you make your approach, trade-offs, and result explicit.", href: "https://www.youtube.com/results?search_query=technical+interview+communication+answers" },
];

export function RecommendedResource() {
  const [active, setActive] = useState(0); const [paused, setPaused] = useState(false); const [completed, setCompleted] = useState<string | null>(null); const [open, setOpen] = useState(false); const count = recommendations.length; const item = recommendations[active];
  const goTo = (index: number) => { if (!count) return; setActive((index + count) % count); setOpen(false); };
  useEffect(() => { if (count < 2 || paused) return; const timer = window.setInterval(() => setActive((current) => (current + 1) % count), 3000); return () => window.clearInterval(timer); }, [active, count, paused]);
  if (!count) return null;
  return <article className="surface-card overflow-hidden rounded-2xl border border-primary/20 bg-card" onMouseEnter={() => setPaused(true)} onMouseLeave={() => setPaused(false)} onFocusCapture={() => setPaused(true)} onBlurCapture={(event) => { if (!event.currentTarget.contains(event.relatedTarget as Node | null)) setPaused(false); }}><div className="grid md:grid-cols-[210px_1fr]"><div className="relative grid min-h-40 place-items-center bg-gradient-to-br from-indigo-600 to-violet-700 text-white"><Play className="size-9 fill-white" aria-hidden="true"/><span className="absolute bottom-3 left-3 rounded bg-black/35 px-2 py-1 text-[11px]">{item.duration}</span></div><div className="p-5"><div className="relative min-h-55 overflow-hidden"><div key={item.title} className="animate-in fade-in slide-in-from-right-2 duration-300"><div className="flex flex-wrap items-center justify-between gap-2"><span className="inline-flex items-center gap-1.5 text-xs font-semibold text-primary"><Sparkles className="size-3.5" aria-hidden="true"/>RECOMMENDED RESOURCE</span><span className="rounded-full bg-rose-50 px-2 py-1 text-[11px] font-semibold text-rose-700">{item.gap}</span></div><h3 className="mt-3 text-lg font-bold">{item.title}</h3><p className="mt-1 text-xs text-muted-foreground">{item.meta} · {item.duration}</p><p className="mt-3 text-sm leading-6 text-muted-foreground">{item.description}</p>{open ? <p className="mt-3 rounded-lg bg-muted p-3 text-xs leading-5 text-muted-foreground"><b className="text-foreground">Why this recommendation?</b> {item.rationale}</p> : null}<div className="mt-4 flex flex-wrap gap-2"><a href={item.href} target="_blank" rel="noopener noreferrer" className="inline-flex h-8 items-center gap-1.5 rounded-lg bg-primary px-3 text-xs font-semibold text-primary-foreground"><ExternalLink className="size-3.5" aria-hidden="true"/>Watch on YouTube</a><Button size="sm" variant="outline" onClick={() => setCompleted(item.title)} disabled={completed === item.title}>{completed === item.title ? <><Check data-icon="inline-start" aria-hidden="true"/>Resource completed</> : "Mark complete"}</Button><button type="button" onClick={() => setOpen((value) => !value)} className="text-xs font-semibold text-primary">{open ? "Hide rationale" : "Why this recommendation?"}</button></div>{completed === item.title ? <p className="mt-3 text-xs font-medium text-emerald-700">Completed · Continue to related practice when you&apos;re ready.</p> : null}</div></div>{count > 1 ? <div className="mt-3 flex items-center justify-between"><div className="flex items-center gap-1" aria-label="Recommendation pagination">{recommendations.map((recommendation, index) => <button key={recommendation.title} type="button" onClick={() => goTo(index)} aria-label={`Show recommendation ${index + 1}: ${recommendation.title}`} aria-current={index === active ? "true" : undefined} className={`size-2.5 rounded-full transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring ${index === active ? "bg-primary" : "bg-border hover:bg-primary/40"}`}/>)}</div><div className="flex gap-1"><button type="button" onClick={() => goTo(active - 1)} aria-label="Previous recommendation" className="grid size-8 place-items-center rounded-lg border border-border text-muted-foreground hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><ChevronLeft className="size-4" aria-hidden="true"/></button><button type="button" onClick={() => goTo(active + 1)} aria-label="Next recommendation" className="grid size-8 place-items-center rounded-lg border border-border text-muted-foreground hover:bg-muted focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-ring"><ChevronRight className="size-4" aria-hidden="true"/></button></div></div> : null}</div></div></article>;
}
