"use client";
import { Check, ExternalLink, Play, Sparkles } from "lucide-react";
import { useState } from "react";
import { Button } from "@/components/ui/button";
export function RecommendedResource() { const [completed, setCompleted] = useState(false); const [open, setOpen] = useState(false); return <article className="surface-card overflow-hidden rounded-2xl border border-primary/20 bg-card"><div className="grid md:grid-cols-[210px_1fr]"><div className="relative grid min-h-40 place-items-center bg-gradient-to-br from-indigo-600 to-violet-700 text-white"><Play className="size-9 fill-white" /><span className="absolute bottom-3 left-3 rounded bg-black/35 px-2 py-1 text-[11px]">18 min</span></div><div className="p-5"><div className="flex flex-wrap items-center justify-between gap-2"><span className="inline-flex items-center gap-1.5 text-xs font-semibold text-primary"><Sparkles className="size-3.5"/>AI RECOMMENDED RESOURCE</span><span className="rounded-full bg-rose-50 px-2 py-1 text-[11px] font-semibold text-rose-700">32-point gap</span></div><h3 className="mt-3 text-lg font-bold">System Design Fundamentals</h3><p className="mt-1 text-xs text-muted-foreground">Curated learning resource · Beginner · 18 min</p><p className="mt-3 text-sm leading-6 text-muted-foreground">System Design is your highest-impact gap. Start with these core concepts before your next guided practice session.</p>{open && <p className="mt-3 rounded-lg bg-muted p-3 text-xs leading-5 text-muted-foreground"><b className="text-foreground">Why this recommendation?</b> Your current System Design confidence is 38% against a 70% role benchmark. This resource focuses on components, scale, and trade-offs—the foundation for the next practice activity.</p>}<div className="mt-4 flex flex-wrap gap-2"><a href="https://www.youtube.com/results?search_query=system+design+fundamentals" target="_blank" rel="noopener noreferrer" className="inline-flex h-8 items-center gap-1.5 rounded-lg bg-primary px-3 text-xs font-semibold text-primary-foreground"><ExternalLink className="size-3.5"/>Watch on YouTube</a><Button size="sm" variant="outline" onClick={() => setCompleted(true)} disabled={completed}>{completed ? <><Check data-icon="inline-start"/>Resource completed</> : "Mark complete"}</Button><button onClick={() => setOpen(!open)} className="text-xs font-semibold text-primary">{open ? "Hide rationale" : "Why this recommendation?"}</button></div>{completed && <p className="mt-3 text-xs font-medium text-emerald-700">Completed · Continue to related practice when you&apos;re ready.</p>}</div></div></article>; }
