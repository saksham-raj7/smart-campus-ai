"use client";
import { useMemo, useState } from "react";
import { ArrowRight, Clock3 } from "lucide-react";
import { AppShell } from "@/components/app-shell/app-shell";
import { companyQuestions, type CompanyQuestion } from "@/lib/career-data";
import { recordReadiness } from "@/lib/readiness";
export default function CompanyBank(){const [company,setCompany]=useState("All"),[topic,setTopic]=useState("All"),[difficulty,setDifficulty]=useState("All"),[selected,setSelected]=useState<CompanyQuestion|null>(null),[started,setStarted]=useState(false),[answer,setAnswer]=useState(""),[submitted,setSubmitted]=useState(false);const items=useMemo(()=>companyQuestions.filter(q=>(company==="All"||q.company===company)&&(topic==="All"||q.topic===topic)&&(difficulty==="All"||q.difficulty===difficulty)),[company,topic,difficulty]);function close(){setSelected(null);setStarted(false);setAnswer("");setSubmitted(false)}function submit(){if(answer.trim().length<20)return;setSubmitted(true);recordReadiness("interview",`${selected?.topic} company-style practice completed`,selected?.topic)}return <AppShell title="Company Question Bank"><div className="mx-auto max-w-7xl"><section><p className="text-xs font-bold tracking-wider text-primary">CURATED INTERVIEW PRACTICE</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Company-style question bank</h2><p className="mt-2 text-sm text-muted-foreground">Practice common interview patterns—these are not official company questions.</p></section><section className="mt-5 flex flex-wrap gap-2 rounded-xl border border-border bg-card p-4">{[[company,setCompany,["All","Google","Microsoft","Amazon","TCS","Accenture","Deloitte"]],[topic,setTopic,["All","DSA","System Design","SQL","React","Behavioral","Communication"]],[difficulty,setDifficulty,["All","Easy","Medium","Hard"]]].map(([value,setter,values],i)=><select key={i} value={value as string} onChange={e=>(setter as (x:string)=>void)(e.target.value)} className="rounded-lg border border-input bg-background px-3 py-2 text-xs">{(values as string[]).map(x=><option key={x}>{x}</option>)}</select>)}</section><div className="mt-5 grid gap-4 lg:grid-cols-2">{items.map(q=><article key={q.id} className="rounded-xl border border-border bg-card p-5 shadow-sm"><div className="flex justify-between text-xs"><span className="font-bold text-primary">{q.company}-style · {q.topic}</span><span>{q.difficulty}</span></div><h3 className="mt-4 text-lg font-semibold">{q.question}</h3><p className="mt-3 text-sm text-muted-foreground">Tests: {q.expectedSkill} · <span className="inline-flex items-center gap-1"><Clock3 className="size-3"/>{q.estimatedTime}</span></p><button onClick={()=>setSelected(q)} className="mt-5 inline-flex items-center gap-2 rounded-lg bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground">Practice question <ArrowRight className="size-4"/></button></article>)}</div>{!items.length&&<p className="mt-8 rounded-xl border border-dashed border-border p-8 text-center text-sm text-muted-foreground">No questions match these filters. Try a different combination.</p>}{selected&&<div className="fixed inset-0 z-50 grid place-items-center bg-foreground/30 p-4 backdrop-blur-sm"><section role="dialog" aria-modal className="w-full max-w-2xl rounded-2xl border border-border bg-card p-6 shadow-xl"><button onClick={close} className="float-right text-sm text-muted-foreground">Close</button>{!started?<><p className="text-xs font-bold tracking-wider text-primary">{selected.company}-STYLE · {selected.topic}</p><h2 className="mt-3 text-2xl font-semibold">{selected.question}</h2><p className="mt-4 rounded-lg bg-muted p-3 text-sm"><b>What we&apos;re looking for:</b> {selected.expectedSkill}</p><p className="mt-3 text-sm text-muted-foreground">Difficulty: {selected.difficulty} · Estimated time: {selected.estimatedTime}</p><button onClick={()=>setStarted(true)} className="mt-6 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Start practice</button></>:submitted?<><p className="text-xs font-bold tracking-wider text-primary">AI EVALUATION</p><h2 className="mt-3 text-3xl font-semibold">82%</h2><div className="mt-4 grid gap-3 sm:grid-cols-2 text-sm"><p className="rounded-lg bg-emerald-50 p-3 text-emerald-800"><b>Strengths</b><br/>✓ Clear approach<br/>✓ Relevant reasoning</p><p className="rounded-lg bg-amber-50 p-3 text-amber-800"><b>Improve</b><br/>△ State complexity<br/>△ Explain edge cases</p></div><p className="mt-4 text-sm leading-6 text-muted-foreground">Your response addresses the core requirement. Make the trade-offs and complexity explicit to make it interview-ready.</p><button onClick={close} className="mt-6 rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground">Return to question bank</button></>:<><p className="text-xs font-bold tracking-wider text-primary">YOUR ANSWER</p><h2 className="mt-3 text-xl font-semibold">{selected.question}</h2><textarea value={answer} onChange={e=>setAnswer(e.target.value)} className="mt-5 min-h-44 w-full rounded-lg border border-input bg-background p-3 text-sm outline-none focus:ring-2 focus:ring-primary/30" placeholder="Explain your approach, assumptions, and complexity…"/><div className="mt-4 flex justify-between"><button onClick={close} className="text-sm font-medium text-muted-foreground">Save Draft</button><button disabled={answer.trim().length<20} onClick={submit} className="rounded-lg bg-primary px-4 py-2 text-sm font-semibold text-primary-foreground disabled:opacity-50">Submit answer</button></div></>}</section></div>}</div></AppShell>}
