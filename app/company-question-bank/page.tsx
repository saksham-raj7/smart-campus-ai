"use client";
import { useMemo, useState } from "react";
import { Building2, Clock3, Search, X } from "lucide-react";
import { AppShell } from "@/components/app-shell/app-shell";
import { companyQuestions, type CompanyQuestion } from "@/lib/career-data";
const companies = ["All", "Google", "Microsoft", "Amazon", "TCS", "Accenture", "Deloitte"]; const topics = ["All", "DSA", "System Design", "JavaScript", "React", "SQL", "Behavioral", "Communication"];
export default function CompanyBank() { const [search, setSearch] = useState(""); const [company, setCompany] = useState("All"); const [topic, setTopic] = useState("All"); const [difficulty, setDifficulty] = useState("All"); const [open, setOpen] = useState<CompanyQuestion | null>(null); const questions = useMemo(() => companyQuestions.filter(q => (company === "All" || q.company === company) && (topic === "All" || q.topic === topic) && (difficulty === "All" || q.difficulty === difficulty) && `${q.question} ${q.company} ${q.topic}`.toLowerCase().includes(search.toLowerCase())), [company, topic, difficulty, search]); return <AppShell title="Company Question Bank"><div className="mx-auto max-w-7xl"><section className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold tracking-wider text-primary">CURATED INTERVIEW PRACTICE</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Company-style question bank</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-muted-foreground">Practice curated interview patterns commonly associated with role expectations—not official company questions.</p></div><span className="rounded-full bg-primary/10 px-3 py-1.5 text-xs font-semibold text-primary">{questions.length} questions matched</span></section><section className="mt-5 rounded-xl border border-border bg-card p-4 shadow-sm"><div className="relative"><Search className="absolute left-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground"/><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Search questions, companies, or skills" className="h-11 w-full rounded-lg border border-input bg-background pl-10 pr-3 text-sm outline-none focus:ring-3 focus:ring-ring/20"/></div><div className="mt-3 flex flex-wrap gap-2"><select aria-label="Company filter" value={company} onChange={e => setCompany(e.target.value)} className="rounded-lg border border-input bg-background px-3 py-2 text-xs">{companies.map(x => <option key={x}>{x}</option>)}</select><select aria-label="Topic filter" value={topic} onChange={e => setTopic(e.target.value)} className="rounded-lg border border-input bg-background px-3 py-2 text-xs">{topics.map(x => <option key={x}>{x}</option>)}</select><select aria-label="Difficulty filter" value={difficulty} onChange={e => setDifficulty(e.target.value)} className="rounded-lg border border-input bg-background px-3 py-2 text-xs">{["All", "Easy", "Medium", "Hard"].map(x => <option key={x}>{x}</option>)}</select></div></section><div className="mt-5 grid gap-4 lg:grid-cols-2">{questions.map(q => <article key={q.id} className="flex flex-col rounded-xl border border-border bg-card p-5 shadow-sm transition hover:-translate-y-0.5 hover:border-primary/30"><div className="flex flex-wrap items-center gap-2"><span className="rounded-md bg-primary/10 px-2 py-1 text-[11px] font-bold text-primary">{q.company}</span><span className="text-xs text-muted-foreground">{q.topic} · {q.difficulty}</span></div><h3 className="mt-4 text-base font-semibold leading-6">{q.question}</h3><div className="mt-5 flex flex-wrap gap-x-5 gap-y-2 border-t border-border pt-4 text-xs text-muted-foreground"><span>Expected: <b className="text-foreground">{q.expectedSkill}</b></span><span className="flex items-center gap-1"><Clock3 className="size-3"/>{q.estimatedTime}</span></div><button onClick={() => setOpen(q)} className="mt-5 w-fit rounded-lg bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground hover:bg-primary/90">Practice question</button></article>)}</div>{questions.length === 0 ? <div className="mt-5 rounded-xl border border-dashed border-border p-12 text-center"><Building2 className="mx-auto size-6 text-primary"/><h3 className="mt-3 font-semibold">No questions match those filters</h3><button onClick={() => { setSearch(""); setCompany("All"); setTopic("All"); setDifficulty("All"); }} className="mt-3 text-sm font-semibold text-primary">Clear filters</button></div> : null}{open ? <div className="fixed inset-0 z-50 grid place-items-center bg-foreground/25 p-4 backdrop-blur-sm"><section role="dialog" aria-modal="true" aria-label="Question practice" className="w-full max-w-xl rounded-2xl border border-border bg-card p-6 shadow-xl"><button onClick={() => setOpen(null)} className="float-right rounded-lg p-1 text-muted-foreground hover:bg-muted" aria-label="Close practice"><X className="size-4"/></button><p className="text-xs font-bold tracking-wider text-primary">{open.company} · {open.topic}</p><h2 className="mt-3 text-xl font-semibold leading-7">{open.question}</h2><p className="mt-4 rounded-lg bg-muted p-3 text-sm"><b>What we&apos;re looking for:</b> {open.expectedSkill}</p><label className="mt-5 block text-sm font-medium">Your practice response<textarea className="mt-2 min-h-32 w-full rounded-lg border border-input bg-background p-3 text-sm outline-none focus:ring-3 focus:ring-ring/20" placeholder="Outline your approach, then explain your reasoning…"/></label><div className="mt-5 flex justify-end gap-2"><button onClick={() => setOpen(null)} className="rounded-lg px-3 py-2 text-sm font-medium hover:bg-muted">Back</button><button onClick={() => setOpen(null)} className="rounded-lg bg-primary px-3 py-2 text-sm font-semibold text-primary-foreground">Save practice draft</button></div></section></div> : null}</div></AppShell>; }
