"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, Check, Route, Sparkles } from "lucide-react";
import { startDemoSession } from "@/lib/auth";

export default function LoginPage() {
  const router = useRouter(); const [signup, setSignup] = useState(false); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [role, setRole] = useState("Software Engineer"); const [error, setError] = useState("");
  function submit(e: React.FormEvent) { e.preventDefault(); if (!email.includes("@") || password.length < 4) { setError("Enter a valid email and a password with at least 4 characters."); return; } startDemoSession({ name: signup && name.trim() ? name.trim() : "Student", email, targetRole: role }); router.replace("/dashboard"); }
  return <main className="grid min-h-dvh bg-slate-50 lg:grid-cols-[1.08fr_.92fr]"><section className="relative hidden overflow-hidden bg-slate-950 p-10 text-white lg:flex lg:flex-col"><div className="absolute -right-24 top-16 size-96 rounded-full bg-indigo-500/20 blur-3xl" /><div className="relative flex items-center gap-3"><span className="flex size-10 items-center justify-center rounded-xl bg-white text-indigo-700"><Route className="size-5" /></span><span><b className="block text-lg">AI Career OS</b><span className="text-[10px] font-bold tracking-[.16em] text-slate-400">CAREER INTELLIGENCE</span></span></div><div className="relative my-auto max-w-xl"><span className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/5 px-3 py-1.5 text-xs text-indigo-200"><Sparkles className="size-3" />AI-POWERED CAREER INTELLIGENCE</span><h1 className="mt-6 text-5xl font-semibold leading-[1.08] tracking-tight">Make every step of your career preparation count.</h1><p className="mt-5 max-w-md text-base leading-7 text-slate-300">Your personal AI-powered career intelligence system—built to help you discover gaps, learn with intent, practice, and get ready.</p><div className="mt-10 flex items-center gap-2 text-sm font-medium text-indigo-100"><span>Discover</span><ArrowRight className="size-3" /><span>Learn</span><ArrowRight className="size-3" /><span>Practice</span><ArrowRight className="size-3" /><span>Improve</span><ArrowRight className="size-3" /><span>Ready</span></div></div><p className="relative text-xs text-slate-400">A focused workspace for students building career confidence.</p></section><section className="flex items-center justify-center p-5 sm:p-10"><div className="w-full max-w-md"><div className="mb-9 flex items-center gap-2 lg:hidden"><span className="flex size-9 items-center justify-center rounded-xl bg-primary text-primary-foreground"><Route className="size-4" /></span><b>AI Career OS</b></div><p className="text-sm font-semibold text-primary">WELCOME TO YOUR WORKSPACE</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">{signup ? "Create your career profile" : "Welcome back"}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{signup ? "Start with a profile—you can refine it anytime." : "Sign in to continue your career momentum."}</p><form onSubmit={submit} className="mt-8 space-y-4">{signup ? <label className="block text-sm font-medium">Name<input value={name} onChange={e => setName(e.target.value)} required className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30" placeholder="Your name" /></label> : null}<label className="block text-sm font-medium">Email<input type="email" value={email} onChange={e => setEmail(e.target.value)} required className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30" placeholder="you@example.com" /></label><label className="block text-sm font-medium">Password<input type="password" value={password} onChange={e => setPassword(e.target.value)} required minLength={4} className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30" placeholder="At least 4 characters" /></label>{signup ? <label className="block text-sm font-medium">Target role<select value={role} onChange={e => setRole(e.target.value)} className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30"><option>Software Engineer</option><option>Data Analyst</option><option>Product Manager</option></select></label> : null}{error ? <p role="alert" className="rounded-lg bg-destructive/10 p-3 text-xs text-destructive">{error}</p> : null}<button className="flex h-11 w-full items-center justify-center gap-2 rounded-lg bg-primary text-sm font-semibold text-primary-foreground transition hover:bg-primary/90">{signup ? "Create account" : "Sign in"}<ArrowRight className="size-4" /></button></form><button type="button" onClick={() => setError("Google sign-in is ready for the authentication API integration. Use email sign-in for this demo.")} className="mt-3 flex h-11 w-full items-center justify-center gap-2 rounded-lg border border-border bg-card text-sm font-medium hover:bg-muted"><span className="grid size-5 place-items-center rounded-full bg-slate-100 text-xs font-bold text-slate-700">G</span>Continue with Google</button><p className="mt-6 text-center text-sm text-muted-foreground">{signup ? "Already have an account?" : "Don’t have an account?"} <button onClick={() => { setSignup(!signup); setError(""); }} className="font-semibold text-primary">{signup ? "Sign in" : "Create account"}</button></p><p className="mt-8 flex items-center justify-center gap-2 text-center text-xs text-muted-foreground"><Check className="size-3 text-primary" />Demo sessions persist securely in this browser.</p></div></section></main>;
}
