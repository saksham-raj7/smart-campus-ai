"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { ArrowRight, Check, Eye, EyeOff, Sparkles } from "lucide-react";
import { startDemoSession } from "@/lib/auth";
import { Brand } from "@/components/app-shell/brand";

export default function LoginPage() {
  const router = useRouter(); const [signup, setSignup] = useState(false); const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [role, setRole] = useState("Software Engineer"); const [error, setError] = useState(""); const [showPassword, setShowPassword] = useState(false); const [isSubmitting, setIsSubmitting] = useState(false);
  function submit(e: React.FormEvent) { e.preventDefault(); if (!email.includes("@") || password.length < 4) { setError("Enter a valid email and a password with at least 4 characters."); return; } setIsSubmitting(true); window.setTimeout(() => { startDemoSession({ name: signup && name.trim() ? name.trim() : "Student", email, targetRole: role }); router.replace("/dashboard"); }, 350); }
  return <main className="grid min-h-dvh bg-slate-50 lg:grid-cols-[1.08fr_.92fr]"><section className="relative hidden overflow-hidden bg-slate-950 p-10 text-white lg:flex lg:flex-col"><div className="absolute -right-24 top-16 size-96 rounded-full bg-indigo-500/25 blur-3xl" /><div className="absolute bottom-0 left-0 size-80 rounded-full bg-violet-500/15 blur-3xl" /><div className="relative"><Brand /></div><div className="relative my-auto max-w-xl"><span className="inline-flex items-center gap-2 rounded-full border border-white/15 bg-white/5 px-3 py-1.5 text-xs text-indigo-200"><Sparkles className="size-3" />AI-POWERED CAREER INTELLIGENCE</span><h1 className="mt-6 text-5xl font-bold leading-[1.08] tracking-[-.04em]">Make every step of your career preparation count.</h1><p className="mt-5 max-w-md text-base leading-7 text-slate-300">Discover the gaps holding you back, build practice momentum, and see your readiness improve.</p><div className="mt-10 flex items-center gap-2 text-sm font-medium text-indigo-100"><span>Assess</span><ArrowRight className="size-3" /><span>Learn</span><ArrowRight className="size-3" /><span>Practice</span><ArrowRight className="size-3" /><span>Ready</span></div></div><p className="relative text-xs text-slate-400">Your private career intelligence workspace.</p></section><section className="flex items-center justify-center p-5 sm:p-10"><div className="w-full max-w-md"><div className="mb-9 lg:hidden"><Brand /></div><p className="text-sm font-semibold text-primary">WELCOME TO YOUR WORKSPACE</p><h2 className="mt-2 text-3xl font-bold tracking-[-.03em]">{signup ? "Create your career profile" : "Welcome back"}</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">{signup ? "Start with a profile—you can refine it anytime." : "Sign in to continue your career momentum."}</p><form onSubmit={submit} className="mt-8 space-y-4">{signup ? <label className="block text-sm font-medium">Name<input value={name} onChange={e => setName(e.target.value)} required className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30" placeholder="Your name" /></label> : null}<label className="block text-sm font-medium">Email<input type="email" value={email} onChange={e => setEmail(e.target.value)} required className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30" placeholder="you@example.com" /></label><label className="block text-sm font-medium">Password<div className="relative mt-1.5"><input type={showPassword ? "text" : "password"} value={password} onChange={e => setPassword(e.target.value)} required minLength={4} className="h-11 w-full rounded-lg border border-input bg-background px-3 pr-10 font-normal outline-none focus:ring-3 focus:ring-ring/30" placeholder="At least 4 characters" /><button type="button" onClick={() => setShowPassword(!showPassword)} className="absolute right-2 top-1/2 -translate-y-1/2 rounded p-1 text-muted-foreground" aria-label={showPassword ? "Hide password" : "Show password"}>{showPassword ? <EyeOff className="size-4" /> : <Eye className="size-4" />}</button></div></label>{signup ? <label className="block text-sm font-medium">Target role<select value={role} onChange={e => setRole(e.target.value)} className="mt-1.5 h-11 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/30"><option>Software Engineer</option><option>Data Analyst</option><option>Product Manager</option></select></label> : null}{error ? <p role="alert" className="rounded-lg bg-destructive/10 p-3 text-xs text-destructive">{error}</p> : null}<button disabled={isSubmitting} className="flex h-11 w-full items-center justify-center gap-2 rounded-lg bg-primary text-sm font-semibold text-primary-foreground transition hover:bg-primary/90 disabled:opacity-70">{isSubmitting ? "Entering workspace…" : signup ? "Create account" : "Sign in"}<ArrowRight className="size-4" /></button></form><p className="mt-6 text-center text-sm text-muted-foreground">{signup ? "Already have an account?" : "Don’t have an account?"} <button onClick={() => { setSignup(!signup); setError(""); }} className="font-semibold text-primary">{signup ? "Sign in" : "Create account"}</button></p><p className="mt-8 flex items-center justify-center gap-2 text-center text-xs text-muted-foreground"><Check className="size-3 text-primary" />Your session stays on this device.</p></div></section></main>;
}
