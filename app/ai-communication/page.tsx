"use client";

import { useState } from "react";
import { ArrowLeft, ArrowRight, Bot, CheckCircle2, ChevronDown, Lightbulb, Mic, PencilLine, Sparkles, Square, WandSparkles } from "lucide-react";
import { useRouter } from "next/navigation";
import { AppShell } from "@/components/app-shell/app-shell";
import { Button } from "@/components/ui/button";
import { cn } from "@/lib/utils";

export type InterviewQuestion = { id: number; text: string };
export type CommunicationScore = { label: "Clarity" | "Structure" | "Confidence"; value: number; tone: string };
export type CoachFeedback = { message: string; suggestions: string[]; strongerOpening: string; scores: CommunicationScore[] };
export type LanguageOption = "English" | "Hindi" | "Hinglish";
export type InterviewSession = { targetRole: string; interviewType: string; difficulty: string; readiness: number; sessionsCompleted: number; confidence: number; focusAreas: number };

const interviewQuestions: InterviewQuestion[] = [
  { id: 1, text: "Tell me about a technical problem you faced and how you solved it." },
  { id: 2, text: "How would you design a scalable system for a large number of users?" },
  { id: 3, text: "Tell me about a time you disagreed with a teammate." },
  { id: 4, text: "How do you approach debugging a difficult issue?" },
  { id: 5, text: "Why are you interested in this Software Engineer role?" },
];
const demoSession: InterviewSession = { targetRole: "Software Engineer", interviewType: "Technical + Behavioral", difficulty: "Adaptive", readiness: 68, sessionsCompleted: 12, confidence: 46, focusAreas: 3 };
const demoFeedback: CoachFeedback = {
  message: "Good structure. Your answer clearly explains the problem and solution. Add measurable impact to make it stronger.",
  scores: [{ label: "Clarity", value: 78, tone: "bg-sky-500" }, { label: "Structure", value: 72, tone: "bg-violet-500" }, { label: "Confidence", value: 64, tone: "bg-amber-500" }],
  suggestions: ["Start with the situation in one sentence.", "Explain your specific contribution.", "End with measurable impact."],
  strongerOpening: "I faced a performance bottleneck in our course project when response times increased as the number of users grew. I profiled the application, identified an inefficient query, and introduced caching to improve the experience.",
};
const demoTranscript = "In a group project, our dashboard became slow as more data was added. I traced the issue to repeated database queries, worked with my teammate to add caching, and reduced the load time significantly.";
const followUpQuestion = "What trade-offs did you consider when choosing your solution?";

function ProgressBar({ value, className = "bg-primary" }: { value: number; className?: string }) {
  return <div className="h-2 overflow-hidden rounded-full bg-muted" role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={value}><div className={cn("h-full rounded-full transition-all duration-300", className)} style={{ width: value + "%" }} /></div>;
}

export default function AiCommunicationPage() {
  const router = useRouter();
  const [questionIndex, setQuestionIndex] = useState(0);
  const [responseMode, setResponseMode] = useState<"idle" | "typing" | "voice">("idle");
  const [isRecording, setIsRecording] = useState(false);
  const [answer, setAnswer] = useState("");
  const [hasFeedback, setHasFeedback] = useState(false);
  const [isFollowUp, setIsFollowUp] = useState(false);
  const [isComplete, setIsComplete] = useState(false);
  const [language, setLanguage] = useState<LanguageOption>("English");
  const currentQuestion = interviewQuestions[questionIndex];
  const activePrompt = isFollowUp ? followUpQuestion : currentQuestion.text;
  const resetAnswerState = () => { setResponseMode("idle"); setIsRecording(false); setAnswer(""); setHasFeedback(false); setIsFollowUp(false); };
  const changeQuestion = (index: number) => { setQuestionIndex(index); resetAnswerState(); };
  const goNext = () => { if (questionIndex === interviewQuestions.length - 1) setIsComplete(true); else changeQuestion(questionIndex + 1); };
  const startAgain = () => { setQuestionIndex(0); resetAnswerState(); setIsComplete(false); };

  return <AppShell title="AI Communication"><div className="mx-auto flex w-full max-w-7xl flex-col gap-5">
    <section className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><div className="mb-2 flex items-center gap-2 text-xs font-medium text-primary"><span className="flex size-5 items-center justify-center rounded-md bg-primary/10"><Sparkles className="size-3" /></span>AI-POWERED COMMUNICATION</div><h1 className="text-2xl font-semibold tracking-tight sm:text-3xl">AI Communication</h1><p className="mt-1.5 max-w-2xl text-sm leading-6 text-muted-foreground">Practice interviews, improve your answers, and build confident communication with AI.</p></div><span className="w-fit rounded-full border border-primary/15 bg-primary/[0.035] px-3 py-1.5 text-xs font-medium text-primary">AI interview coach</span></section>
    <section className="rounded-xl border border-primary/15 bg-card p-5 shadow-sm sm:p-6"><div className="flex flex-col gap-5 lg:flex-row lg:items-end lg:justify-between"><div className="max-w-xl"><div className="flex items-center gap-2 text-sm font-medium"><span className="flex size-7 items-center justify-center rounded-lg bg-primary/10 text-primary"><WandSparkles className="size-4" /></span>Interview readiness</div><p className="mt-3 text-4xl font-semibold tracking-tight">{demoSession.readiness}%</p><p className="mt-1 text-sm text-muted-foreground">Your communication is improving. Focus on structure, clarity, and confidence.</p><div className="mt-4"><ProgressBar value={demoSession.readiness} /></div></div><div className="grid grid-cols-3 divide-x divide-border border-t border-border pt-4 lg:min-w-100 lg:border-t-0 lg:pt-0"><Metric value={demoSession.sessionsCompleted} label="Sessions completed" /><Metric value={demoSession.confidence + "%"} label="Communication confidence" /><Metric value={demoSession.focusAreas} label="Focus areas" /></div></div></section>
    <section className="grid gap-5 xl:grid-cols-[minmax(0,1fr)_300px]">
      <article className="rounded-xl border border-border bg-card shadow-sm">{isComplete ? <Summary onStart={startAgain} onLearn={() => router.push("/learn-practice")} /> : <>
        <div className="border-b border-border p-5 sm:p-6"><div className="flex flex-wrap items-start justify-between gap-3"><div><div className="flex items-center gap-2 text-sm font-semibold"><span className="flex size-7 items-center justify-center rounded-lg bg-primary/10 text-primary"><Bot className="size-4" /></span>AI Mock Interview</div><p className="mt-1.5 text-xs text-muted-foreground">A local demo workspace — voice recording does not access your microphone.</p></div><span className="rounded-md bg-muted px-2 py-1 text-[11px] font-medium text-muted-foreground">Question {questionIndex + 1} of 5</span></div><dl className="mt-5 grid gap-3 text-sm sm:grid-cols-3"><Info label="Target role" value={demoSession.targetRole} /><Info label="Interview type" value={demoSession.interviewType} /><Info label="Difficulty" value={demoSession.difficulty} /></dl><div className="mt-5"><ProgressBar value={((questionIndex + 1) / 5) * 100} /></div></div>
        <div className="p-5 sm:p-6"><p className="text-xs font-medium uppercase tracking-[0.12em] text-primary">{isFollowUp ? "AI follow-up" : "Current question"}</p><h2 className="mt-2 max-w-3xl text-lg font-semibold leading-7 tracking-tight sm:text-xl">{activePrompt}</h2>
          {responseMode === "idle" && <div className="mt-6 flex flex-col gap-3 sm:flex-row"><Button size="lg" onClick={() => { setResponseMode("voice"); setIsRecording(true); }}><Mic data-icon="inline-start" />Answer with voice</Button><Button size="lg" variant="outline" onClick={() => setResponseMode("typing")}><PencilLine data-icon="inline-start" />Type your answer</Button></div>}
          {responseMode === "voice" && <div className="mt-6 rounded-xl border border-primary/20 bg-primary/[0.035] p-4"><div className="flex flex-wrap items-center justify-between gap-3"><div className="flex items-center gap-3"><span className={cn("flex size-9 items-center justify-center rounded-full bg-primary/10 text-primary", isRecording && "animate-pulse")}><Mic className="size-4" /></span><div><p className="text-sm font-medium">{isRecording ? "Recording..." : "Demo transcript ready"}</p><p className="text-xs text-muted-foreground">{isRecording ? "Simulated recording only — no microphone permission requested." : "Your response is ready for AI feedback."}</p></div></div>{isRecording && <Button variant="outline" onClick={() => { setIsRecording(false); setAnswer(demoTranscript); setHasFeedback(true); }}><Square data-icon="inline-start" />Stop recording</Button>}</div>{!isRecording && answer && <p className="mt-4 border-t border-primary/15 pt-4 text-sm leading-6 text-muted-foreground">“{answer}”</p>}</div>}
          {responseMode === "typing" && <div className="mt-6"><label htmlFor="interview-answer" className="text-sm font-medium">Your answer</label><textarea id="interview-answer" value={answer} onChange={(event) => setAnswer(event.target.value)} placeholder="Type your answer here..." className="mt-2 min-h-32 w-full resize-y rounded-lg border border-input bg-background p-3 text-sm leading-6 outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50" /><div className="mt-3 flex flex-wrap gap-3"><Button onClick={() => { if (answer.trim()) setHasFeedback(true); }} disabled={!answer.trim()}>Submit answer</Button><Button variant="ghost" onClick={resetAnswerState}>Cancel</Button></div></div>}
          {hasFeedback && <Feedback onImprove={() => { setResponseMode("typing"); setAnswer(demoFeedback.strongerOpening); }} onFollowUp={() => { setIsFollowUp(true); setHasFeedback(false); setResponseMode("typing"); setAnswer(""); }} onSkip={goNext} />}
        </div>
        <div className="flex flex-col gap-3 border-t border-border bg-muted/30 p-4 sm:flex-row sm:items-center sm:justify-between sm:px-6"><p className="text-sm text-muted-foreground">Question progress: <span className="font-medium text-foreground">{questionIndex + 1} / 5</span></p><div className="flex flex-wrap gap-2"><Button variant="outline" size="sm" disabled={questionIndex === 0} onClick={() => changeQuestion(questionIndex - 1)}><ArrowLeft data-icon="inline-start" />Previous</Button><Button size="sm" onClick={goNext}>Next question<ArrowRight data-icon="inline-end" /></Button><Button variant="destructive" size="sm" onClick={() => setIsComplete(true)}>End interview</Button></div></div>
      </>}</article>
      <Coach language={language} onLanguage={setLanguage} />
    </section>
  </div></AppShell>;
}

function Metric({ value, label }: { value: string | number; label: string }) { return <div className="px-3 first:pl-0 last:pr-0"><p className="text-xl font-semibold">{value}</p><p className="mt-0.5 text-xs text-muted-foreground">{label}</p></div>; }
function Info({ label, value }: { label: string; value: string }) { return <div><dt className="text-xs text-muted-foreground">{label}</dt><dd className="mt-1 font-medium">{value}</dd></div>; }
function Feedback({ onImprove, onFollowUp, onSkip }: { onImprove: () => void; onFollowUp: () => void; onSkip: () => void }) { return <section aria-live="polite" className="mt-6 border-t border-border pt-6"><div className="flex items-center gap-2 text-sm font-semibold"><Sparkles className="size-4 text-primary" />AI feedback</div><p className="mt-3 rounded-lg border border-primary/15 bg-primary/[0.035] p-3 text-sm leading-6 text-muted-foreground">{demoFeedback.message}</p><div className="mt-5 grid gap-4 sm:grid-cols-3">{demoFeedback.scores.map((score) => <div key={score.label}><div className="mb-1.5 flex justify-between text-xs"><span className="font-medium">{score.label}</span><span className="text-muted-foreground">{score.value}%</span></div><ProgressBar value={score.value} className={score.tone} /></div>)}</div><div className="mt-6 grid gap-4 lg:grid-cols-[1fr_0.9fr]"><div><h3 className="text-sm font-semibold">AI suggestions</h3><ol className="mt-3 space-y-2 text-sm text-muted-foreground">{demoFeedback.suggestions.map((suggestion, index) => <li key={suggestion} className="flex gap-2"><span className="font-medium text-primary">{index + 1}.</span>{suggestion}</li>)}</ol></div><div className="rounded-lg border border-border bg-muted/50 p-4"><p className="text-xs font-medium text-muted-foreground">Suggested stronger opening</p><p className="mt-2 text-sm leading-6">“{demoFeedback.strongerOpening}”</p><Button variant="outline" size="sm" className="mt-3" onClick={onImprove}>Try the improved answer</Button></div></div><div className="mt-6 rounded-xl border border-primary/15 bg-primary/[0.025] p-4"><p className="text-sm font-semibold">AI follow-up</p><p className="mt-1 text-xs text-muted-foreground">Your interviewer wants to go deeper.</p><p className="mt-3 text-sm font-medium">{followUpQuestion}</p><div className="mt-4 flex flex-wrap gap-3"><Button size="sm" onClick={onFollowUp}>Answer follow-up</Button><Button size="sm" variant="outline" onClick={onSkip}>Skip</Button></div></div></section>; }
function Summary({ onStart, onLearn }: { onStart: () => void; onLearn: () => void }) { return <div className="p-5 sm:p-7"><span className="flex size-11 items-center justify-center rounded-xl bg-primary/10 text-primary"><CheckCircle2 className="size-5" /></span><h2 className="mt-5 text-xl font-semibold tracking-tight">Interview complete</h2><p className="mt-1 text-sm text-muted-foreground">Here&apos;s how you performed.</p><div className="mt-6 grid gap-3 sm:grid-cols-4"><div className="rounded-lg border border-primary/15 bg-primary/[0.035] p-4"><p className="text-3xl font-semibold text-primary">74%</p><p className="mt-1 text-xs text-muted-foreground">Overall communication</p></div>{demoFeedback.scores.map((score) => <div key={score.label} className="rounded-lg border border-border p-4"><p className="text-xl font-semibold">{score.value}%</p><p className="mt-1 text-xs text-muted-foreground">{score.label}</p></div>)}</div><div className="mt-5 grid gap-3 sm:grid-cols-2"><div className="rounded-lg bg-muted/70 p-4"><p className="text-xs font-medium text-muted-foreground">Your strongest area</p><p className="mt-1 text-sm font-semibold">Clarity</p></div><div className="rounded-lg bg-muted/70 p-4"><p className="text-xs font-medium text-muted-foreground">Best next step</p><p className="mt-1 text-sm font-semibold">Practice structured answers with AI.</p></div></div><div className="mt-6 flex flex-wrap gap-3"><Button onClick={onStart}><Mic data-icon="inline-start" />Start another interview</Button><Button variant="outline" onClick={onLearn}>Back to Learn &amp; Practice</Button></div></div>; }
function Coach({ language, onLanguage }: { language: LanguageOption; onLanguage: (language: LanguageOption) => void }) { return <aside className="space-y-5"><section className="rounded-xl border border-primary/20 bg-primary/[0.035] p-5"><div className="flex items-center gap-2 text-sm font-semibold text-primary"><Bot className="size-4" />AI Coach</div><p className="mt-1 text-sm text-muted-foreground">Live guidance based on your communication.</p><div className="mt-5 rounded-lg bg-card/80 p-3"><p className="text-xs text-muted-foreground">Current focus</p><p className="mt-1 text-sm font-semibold">Answer structure</p></div><dl className="mt-5 space-y-3 text-sm"><CoachStatus label="Clarity" status="Good" color="bg-emerald-50 text-emerald-700" /><CoachStatus label="Structure" status="Needs work" color="bg-amber-50 text-amber-700" /><CoachStatus label="Confidence" status="Improving" color="bg-sky-50 text-sky-700" /></dl><div className="mt-5 border-t border-primary/15 pt-5"><div className="flex items-center gap-2 text-sm font-semibold"><Lightbulb className="size-4 text-primary" />Coach tip</div><p className="mt-2 text-sm leading-6 text-muted-foreground">Use the STAR structure for behavioral questions: Situation → Task → Action → Result.</p></div><div className="mt-5 border-t border-primary/15 pt-5"><label htmlFor="coaching-language" className="text-sm font-medium">Language</label><div className="relative mt-2"><select id="coaching-language" value={language} onChange={(event) => onLanguage(event.target.value as LanguageOption)} className="h-9 w-full appearance-none rounded-lg border border-input bg-card px-3 pr-8 text-sm outline-none focus-visible:border-ring focus-visible:ring-3 focus-visible:ring-ring/50"><option>English</option><option>Hindi</option><option>Hinglish</option></select><ChevronDown className="pointer-events-none absolute right-3 top-1/2 size-4 -translate-y-1/2 text-muted-foreground" /></div><p className="mt-2 text-xs text-muted-foreground">AI coaching language: {language}</p></div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><div className="flex items-center justify-between gap-3"><h2 className="text-sm font-semibold">Speak naturally</h2><span className="rounded-md bg-primary/10 px-2 py-1 text-[11px] font-medium text-primary">Multilingual AI</span></div><p className="mt-2 text-sm leading-6 text-muted-foreground">Practice in the language you&apos;re most comfortable with. Your responses can later be translated and evaluated by AI.</p><div className="mt-4 flex flex-wrap gap-2">{(["English", "Hindi", "Hinglish"] as LanguageOption[]).map((item) => <button key={item} type="button" onClick={() => onLanguage(item)} className={cn("rounded-md border px-2.5 py-1 text-xs font-medium transition-colors focus-visible:outline-none focus-visible:ring-3 focus-visible:ring-ring/50", language === item ? "border-primary/30 bg-primary/10 text-primary" : "border-border bg-background text-muted-foreground hover:border-primary/30")}>{item}</button>)}</div></section></aside>; }
function CoachStatus({ label, status, color }: { label: string; status: string; color: string }) { return <div className="flex items-center justify-between"><dt>{label}</dt><dd className={cn("rounded-md px-2 py-1 text-xs font-medium", color)}>{status}</dd></div>; }
