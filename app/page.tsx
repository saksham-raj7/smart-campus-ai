"use client";
import Link from "next/link";
import { ArrowRight, Sparkles, Target, TrendingUp } from "lucide-react";
import { AppShell } from "@/components/app-shell/app-shell";
import { SkillRadar } from "@/components/ui/skill-radar";
import { score, useReadiness } from "@/lib/readiness";
export default function Home(){const data=useReadiness();const readiness=score(data);const top=[...data.skills].sort((a,b)=>(b.target-b.current)-(a.target-a.current))[0];const recent=data.history.slice(-3).reverse();return <AppShell title="Dashboard"><div className="mx-auto flex max-w-7xl flex-col gap-5"><section className="flex justify-between gap-4"><div><p className="text-xs font-bold tracking-wider text-primary">YOUR CAREER SIGNAL</p><h2 className="mt-2 text-3xl font-semibold tracking-tight">Your next move is clear.</h2><p className="mt-1 text-sm text-muted-foreground">Every learning and practice result updates this shared readiness signal.</p></div><Link href="/progress-readiness" className="text-sm font-semibold text-primary">View details →</Link></section><section className="grid gap-4 xl:grid-cols-[1.2fr_.8fr]"><button onClick={()=>document.getElementById("breakdown")?.scrollIntoView({behavior:"smooth"})} className="text-left surface-card rounded-2xl border border-primary/20 bg-primary/[.035] p-6"><div className="flex items-center gap-2 text-sm font-semibold"><Target className="size-4 text-primary"/>Career readiness</div><p className="mt-5 font-mono text-6xl font-bold">{readiness}<span className="text-2xl text-muted-foreground">%</span></p><p className="mt-2 text-sm text-muted-foreground">Updated from your assessment, learning, coding, and interview signals.</p><div className="mt-5 h-2 rounded-full bg-muted"><div className="h-full rounded-full bg-primary" style={{width:`${readiness}%`}}/></div></button><article className="rounded-2xl bg-gradient-to-br from-indigo-600 to-violet-700 p-6 text-white"><div className="flex gap-2 text-sm font-semibold text-indigo-100"><Sparkles className="size-4"/>NEXT BEST ACTION</div><h3 className="mt-4 text-xl font-bold">{top.name} is your highest-impact gap.</h3><p className="mt-2 text-sm text-indigo-100">Current {top.current}% · Target {top.target}% · {top.target-top.current}-point gap</p><Link href="/career-skill-gap" className="mt-5 inline-flex rounded-lg bg-white px-3 py-2 text-sm font-semibold text-indigo-700">Build this skill <ArrowRight className="ml-1 size-4"/></Link></article></section><section className="grid gap-4 lg:grid-cols-2"><article className="rounded-2xl border border-border bg-card p-5"><h3 className="font-semibold">Skill profile</h3><SkillRadar className="mt-2" points={data.skills.map(s=>({label:s.name.replace("JavaScript & TypeScript","JS/TS").replace("Interview Communication","Comms"),current:s.current,target:s.target}))}/></article><article id="breakdown" className="rounded-2xl border border-border bg-card p-5"><h3 className="font-semibold">Recent readiness changes</h3><p className="mt-1 text-xs text-muted-foreground">Scores change only after completed, evaluated activity.</p><div className="mt-4 space-y-3">{recent.map((item,i)=><div key={`${item.date}-${i}`} className="flex justify-between border-b border-border pb-3 text-sm"><span>{item.reason}</span><span className="font-semibold text-primary">{item.impact>0?`+${item.impact}`:"Baseline"}</span></div>)}</div><Link href="/progress-readiness" className="mt-4 inline-flex text-sm font-semibold text-primary">See calculation <TrendingUp className="ml-1 size-4"/></Link></article></section></div></AppShell>}
