"use client";
import { Download, LockKeyhole, ShieldCheck, UsersRound } from "lucide-react";
import { useEffect, useState } from "react";
import { AppShell } from "@/components/app-shell/app-shell";
import { getSession } from "@/lib/auth";
const rows=[{id:"STU-1042",readiness:82,role:"Software Engineer",strength:"JavaScript",gap:"System Design",status:"Ready"},{id:"STU-1098",readiness:74,role:"Software Engineer",strength:"React",gap:"Data Structures",status:"In progress"},{id:"STU-1135",readiness:48,role:"Data Analyst",strength:"Spreadsheets",gap:"Python",status:"At risk"}];
export default function AdminPage(){const [authorized,setAuthorized]=useState<boolean|null>(null);const [filter,setFilter]=useState("All");useEffect(()=>{const t=window.setTimeout(()=>setAuthorized(getSession()?.role==="placement_officer"),0);return()=>window.clearTimeout(t)},[]);if(authorized===null)return <div className="grid min-h-dvh place-items-center"><span className="size-8 animate-spin rounded-full border-2 border-primary border-t-transparent"/></div>;if(!authorized)return <AppShell title="Placement Command Center"><main className="mx-auto grid min-h-[70dvh] max-w-lg place-items-center text-center"><div><span className="mx-auto grid size-12 place-items-center rounded-2xl bg-destructive/10 text-destructive"><LockKeyhole className="size-5"/></span><h2 className="mt-5 text-xl font-bold">Admin access required</h2><p className="mt-2 text-sm leading-6 text-muted-foreground">Switch to the Placement Officer workspace to access institutional career intelligence.</p></div></main></AppShell>;const visible=filter==="All"?rows:rows.filter(row=>row.status===filter);function exportCsv(){const csv=["Student,Readiness,Target role,Strength,Gap,Status",...visible.map(r=>`${r.id},${r.readiness},${r.role},${r.strength},${r.gap},${r.status}`)].join("\n");const a=document.createElement("a");a.href=URL.createObjectURL(new Blob([csv],{type:"text/csv"}));a.download="skillora-shortlist.csv";a.click();URL.revokeObjectURL(a.href)}return <AppShell title="Placement Command Center"><div className="mx-auto max-w-7xl"><section className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold tracking-wider text-primary">SKILLORA · PLACEMENT INTELLIGENCE</p><h2 className="mt-1 text-3xl font-bold tracking-tight">Cohort readiness, at a glance.</h2><p className="mt-2 text-sm text-muted-foreground">Institutional career intelligence with privacy-conscious student signals.</p></div><span className="inline-flex w-fit items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-700"><ShieldCheck className="size-3.5"/>PLACEMENT OFFICER</span></section><section className="mt-5 grid gap-4 sm:grid-cols-2 xl:grid-cols-4">{[["Overall cohort readiness","68%","+5 points this period"],["Placement-ready students","29%","36 students above 75%"],["Students in progress","52%","66 active learners"],["At-risk students","19%","Top gap: System Design"]].map(([label,value,detail])=><article key={label} className="surface-card rounded-2xl border border-border bg-card p-5"><p className="text-xs text-muted-foreground">{label}</p><p className="mt-3 font-mono text-3xl font-bold">{value}</p><p className="mt-2 text-xs text-primary">{detail}</p></article>)}</section><section className="mt-5 grid gap-4 lg:grid-cols-2"><article className="surface-card rounded-2xl border border-border bg-card p-5"><h3 className="font-semibold">Cohort readiness distribution</h3><p className="mt-1 text-xs text-muted-foreground">Live cohort view · Synced recently</p><div className="mt-6 flex h-36 items-end gap-5">{[["Ready",29,"bg-emerald-500"],["In progress",52,"bg-amber-400"],["At risk",19,"bg-rose-400"]].map(([label,value,color])=><div key={String(label)} className="flex flex-1 flex-col items-center gap-2"><i className={`w-full rounded-t ${color}`} style={{height:`${Number(value)*1.55}%`}}/><span className="text-center text-xs text-muted-foreground">{label}<br/>{value}%</span></div>)}</div></article><article className="surface-card rounded-2xl border border-border bg-card p-5"><h3 className="font-semibold">Top skill gaps</h3><p className="mt-1 text-xs text-muted-foreground">Most frequently under-benchmark signals.</p><div className="mt-5 space-y-4">{[["System Design",42],["Data Structures",36],["Communication",29],["React",18]].map(([name,value])=><div key={String(name)}><div className="flex justify-between text-sm"><span>{name}</span><b>{value}%</b></div><div className="mt-2 h-2 rounded-full bg-muted"><i className="block h-full rounded-full bg-primary" style={{width:`${value}%`}}/></div></div>)}</div></article></section><section className="surface-card mt-5 rounded-2xl border border-border bg-card p-5"><div className="flex flex-col justify-between gap-3 sm:flex-row sm:items-center"><div><h3 className="flex items-center gap-2 font-semibold"><UsersRound className="size-4 text-primary"/>Student shortlist</h3><p className="mt-1 text-xs text-muted-foreground">Anonymized identifiers in this demo workspace.</p></div><div className="flex gap-2"><select value={filter} onChange={e=>setFilter(e.target.value)} className="rounded-lg border border-input bg-background px-3 text-xs"><option>All</option><option>Ready</option><option>In progress</option><option>At risk</option></select><button onClick={exportCsv} className="inline-flex items-center gap-2 rounded-lg bg-primary px-3 py-2 text-xs font-semibold text-primary-foreground"><Download className="size-3.5"/>Export CSV</button></div></div><div className="mt-5 overflow-x-auto"><table className="w-full min-w-160 text-left text-sm"><thead className="border-b border-border text-xs text-muted-foreground"><tr><th className="pb-3">Student</th><th className="pb-3">Readiness</th><th className="pb-3">Role</th><th className="pb-3">Strength</th><th className="pb-3">Gap</th><th className="pb-3">Status</th></tr></thead><tbody>{visible.map(r=><tr key={r.id} className="border-b border-border/70"><td className="py-3 font-mono text-xs">{r.id}</td><td className="py-3 font-semibold">{r.readiness}%</td><td className="py-3">{r.role}</td><td className="py-3">{r.strength}</td><td className="py-3">{r.gap}</td><td className="py-3"><span className="rounded-full bg-muted px-2 py-1 text-xs">{r.status}</span></td></tr>)}</tbody></table></div></section></div></AppShell>}
