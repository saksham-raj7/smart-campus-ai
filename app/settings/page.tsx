"use client";
import { Check, LockKeyhole, LogOut, Save } from "lucide-react";
import { useState } from "react";
import { useRouter } from "next/navigation";
import { AppShell } from "@/components/app-shell/app-shell";
import { demoUser } from "@/lib/career-data";
import { endSession } from "@/lib/auth";
import { ThemeToggle } from "@/components/app-shell/theme-toggle";
export default function SettingsPage() { const router=useRouter(); const [saved,setSaved]=useState(false); const [form,setForm]=useState({name:demoUser.name,role:demoUser.targetRole,status:demoUser.studentStatus,education:demoUser.education,style:"Encouraging & direct",language:"English"}); const change=(key:keyof typeof form,value:string)=>setForm(v=>({...v,[key]:value})); function save(){setSaved(false);window.setTimeout(()=>setSaved(true),350)} return <AppShell title="Settings"><div className="mx-auto max-w-4xl"><section className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold tracking-wider text-primary">PERSONALIZE YOUR WORKSPACE</p><h2 className="mt-1 text-3xl font-bold tracking-tight">Settings</h2><p className="mt-2 text-sm text-muted-foreground">Control your Skillora career context and coaching experience.</p></div><button onClick={save} className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Save className="size-4"/>Save settings</button></section>{saved&&<p role="status" className="mt-4 flex items-center gap-2 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700"><Check className="size-4"/>Settings saved for this device.</p>}<div className="mt-5 space-y-4"><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">Profile</h3><div className="mt-4 grid gap-4 sm:grid-cols-2">{([["Name","name"],["Student status","status"],["Target role","role"],["Education","education"]] as const).map(([label,key])=><label key={key} className="text-sm font-medium">{label}<input value={form[key]} onChange={e=>change(key,e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/20"/></label>)}</div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">Appearance</h3><p className="mt-1 text-xs text-muted-foreground">Choose light, dark, or a system-matched workspace.</p><div className="mt-3"><ThemeToggle /></div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">AI preferences</h3><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Coaching style<select value={form.style} onChange={e=>change("style",e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal"><option>Encouraging & direct</option><option>Detailed & analytical</option><option>Concise & action-oriented</option></select></label><label className="text-sm font-medium">AI language<select value={form.language} onChange={e=>change("language",e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal"><option>English</option><option>Hindi</option><option>Hinglish</option></select></label></div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><div className="flex items-start justify-between gap-4"><div><div className="flex items-center gap-2"><LockKeyhole className="size-4 text-primary" aria-hidden="true"/><h3 className="font-semibold">Anonymous Peer Benchmarking</h3></div><p className="mt-1 text-sm leading-6 text-muted-foreground">Allow Skillora to use your anonymized learning and assessment activity to improve cohort-level insights.</p><p className="mt-2 text-xs text-muted-foreground">Preference controls are awaiting backend integration.</p></div><button type="button" disabled aria-label="Anonymous Peer Benchmarking is unavailable until backend integration is complete" className="shrink-0 rounded-full border border-border bg-muted px-3 py-1.5 text-xs font-medium text-muted-foreground disabled:cursor-not-allowed">Unavailable</button></div></section><section className="rounded-xl border border-destructive/20 bg-card p-5 shadow-sm"><h3 className="font-semibold">Account</h3><p className="mt-1 text-sm text-muted-foreground">End your current session on this device.</p><button onClick={()=>{endSession();router.replace("/login")}} className="mt-4 inline-flex items-center gap-2 rounded-lg border border-destructive/25 px-3 py-2 text-sm font-semibold text-destructive hover:bg-destructive/5"><LogOut className="size-4"/>Log out</button></section></div></div></AppShell>; }
