"use client";
import { useState } from "react";
import { Check, LogOut, Save } from "lucide-react";
import { AppShell } from "@/components/app-shell/app-shell";
import { demoUser } from "@/lib/career-data";
import { endSession } from "@/lib/auth";
import { useRouter } from "next/navigation";
function Toggle({ checked, onChange, label, detail }: { checked: boolean; onChange: () => void; label: string; detail: string }) { return <label className="flex cursor-pointer items-center justify-between gap-4 py-3"><span><span className="block text-sm font-medium">{label}</span><span className="mt-0.5 block text-xs leading-5 text-muted-foreground">{detail}</span></span><button type="button" role="switch" aria-checked={checked} onClick={onChange} className={checked ? "relative h-6 w-11 rounded-full bg-primary transition" : "relative h-6 w-11 rounded-full bg-muted-foreground/30 transition"}><span className={checked ? "absolute top-1 size-4 translate-x-6 rounded-full bg-white transition" : "absolute left-1 top-1 size-4 rounded-full bg-white transition"}/></button></label> }
export default function SettingsPage() { const router = useRouter(); const [saved, setSaved] = useState(false); const [form, setForm] = useState({ name: demoUser.name, role: demoUser.targetRole, status: demoUser.studentStatus, education: demoUser.education, domains: "Product engineering, Frontend", level: "Final-year student", style: "Encouraging & direct", language: "English" }); const [toggles, setToggles] = useState({ learning: true, interview: true, progress: true, recommendations: true, anonymous: true }); const update = (key: keyof typeof form, value: string) => setForm(x => ({ ...x, [key]: value })); const flip = (key: keyof typeof toggles) => setToggles(x => ({ ...x, [key]: !x[key] })); function save() { setSaved(false); setTimeout(() => setSaved(true), 350); } return <AppShell title="Settings"><div className="mx-auto max-w-4xl"><section className="flex flex-col justify-between gap-3 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold tracking-wider text-primary">PERSONALIZE YOUR WORKSPACE</p><h2 className="mt-1 text-3xl font-semibold tracking-tight">Settings</h2><p className="mt-2 text-sm text-muted-foreground">Control the career context and coaching experience you see in AI Career OS.</p></div><button onClick={save} className="inline-flex items-center justify-center gap-2 rounded-lg bg-primary px-4 py-2.5 text-sm font-semibold text-primary-foreground"><Save className="size-4"/>Save settings</button></section>{saved ? <p role="status" className="mt-4 flex items-center gap-2 rounded-lg border border-emerald-200 bg-emerald-50 p-3 text-sm text-emerald-700"><Check className="size-4"/>Preferences saved locally for this browser demo.</p> : null}<div className="mt-5 space-y-4"><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">Profile</h3><div className="mt-4 grid gap-4 sm:grid-cols-2">{([ ["Name", "name"], ["Student status", "status"], ["Target role", "role"], ["Education", "education"] ] as const).map(([label, key]) => <label key={key} className="text-sm font-medium">{label}<input value={form[key]} onChange={e => update(key, e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/20"/></label>)}</div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">Career preferences</h3><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Preferred domains<input value={form.domains} onChange={e => update("domains", e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none focus:ring-3 focus:ring-ring/20"/></label><label className="text-sm font-medium">Experience level<select value={form.level} onChange={e => update("level", e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none"><option>Final-year student</option><option>Pre-final year</option><option>Early career</option></select></label></div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">AI preferences</h3><div className="mt-4 grid gap-4 sm:grid-cols-2"><label className="text-sm font-medium">Coaching style<select value={form.style} onChange={e => update("style", e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none"><option>Encouraging & direct</option><option>Detailed & analytical</option><option>Concise & action-oriented</option></select></label><label className="text-sm font-medium">AI language<select value={form.language} onChange={e => update("language", e.target.value)} className="mt-1.5 h-10 w-full rounded-lg border border-input bg-background px-3 font-normal outline-none"><option>English</option><option>Hindi</option><option>Hinglish</option></select></label></div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">Notifications</h3><div className="divide-y divide-border"><Toggle checked={toggles.learning} onChange={() => flip("learning")} label="Learning reminders" detail="A gentle nudge for your planned learning sessions."/><Toggle checked={toggles.interview} onChange={() => flip("interview")} label="Interview reminders" detail="Reminders before scheduled mock practice."/><Toggle checked={toggles.progress} onChange={() => flip("progress")} label="Progress updates" detail="Weekly readiness and milestone summaries."/><Toggle checked={toggles.recommendations} onChange={() => flip("recommendations")} label="AI recommendations" detail="New high-impact actions from your career coach."/></div></section><section className="rounded-xl border border-border bg-card p-5 shadow-sm"><h3 className="font-semibold">Privacy</h3><Toggle checked={toggles.anonymous} onChange={() => flip("anonymous")} label="Anonymous peer insights" detail="Include your aggregated activity in privacy-safe career trends."/></section><section className="rounded-xl border border-destructive/20 bg-card p-5 shadow-sm"><h3 className="font-semibold">Account</h3><p className="mt-1 text-sm text-muted-foreground">End this local demo session on this device.</p><button onClick={() => { endSession(); router.replace("/login"); }} className="mt-4 inline-flex items-center gap-2 rounded-lg border border-destructive/25 px-3 py-2 text-sm font-semibold text-destructive hover:bg-destructive/5"><LogOut className="size-4"/>Log out</button></section></div></div></AppShell>; }
